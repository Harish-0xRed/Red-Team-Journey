# Daily Log - 04-07-2026

## 📚 Today's Learning

### Linux Essentials - Bash Scripting Practice (Chapter 11 Revision)

Instead of learning new theory, today was dedicated to strengthening Bash fundamentals through practical scripting.

### 🖥️ Bash Scripts Created

- Created and executed the first Bash script successfully.
- Practiced correct Shebang (`#!/bin/bash`).
- Learned the difference between running scripts with `sh script.sh` and `./script.sh`.
- Understood why `chmod +x` is required before executing a script directly.

### 📦 Variables

- Created variables and displayed their values.
- Learned the difference between assigning a value and accessing it using `$`.
- Practiced variable reassignment and understood how Bash updates variable values in memory.

### ⌨️ User Input

- Used the `read` command to collect user input.
- Built interactive scripts that accepted user information.
- Combined `read` with variables and `echo`.

### 📨 Script Arguments

- Learned special variables:
  - `$0` → Script name
  - `$1`, `$2`, `$3` → Command-line arguments
- Built scripts that accepted information directly from the terminal without using `read`.

### 🤔 Conditionals

- Practiced `if`, `else`, and string comparison (`=`).
- Used the `-a` (AND) operator to validate multiple conditions.
- Understood that Bash decisions are based on conditions evaluating to true or false.

### 💻 Mini Projects

Completed multiple practical Bash programs:

- Welcome Script
- Variable Practice Script
- User Information Form
- Student Details using Command-Line Arguments
- Basic Login System
- Linux User Information Program

### 🐞 Debugging

- Fixed execute permission issues using `chmod +x`.
- Solved Bash syntax errors caused by missing spaces after `echo`.
- Practiced reading terminal error messages and correcting scripts independently.

---

## 💡 Key Lessons Learned

- Bash scripts execute line by line from top to bottom.
- Variables act like memory boxes that store values.
- `read` stores values entered by the user.
- Command-line arguments allow scripts to receive input without prompting the user.
- `if` statements make Bash scripts capable of making decisions.
- Debugging and understanding errors are more valuable than memorizing syntax.

---

## 🎯 Tomorrow's Goal

- Complete Bash Loops (`for` and `while`).
- Build a larger Bash project combining variables, user input, conditionals, arguments, and loops.
- Continue strengthening Bash scripting before moving to Linux Essentials Chapter 12.
