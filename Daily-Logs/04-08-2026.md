# Daily Log - 04-08-2026

## 📚 Today's Learning

### Linux Terminal Practice

#### Bandit Level 17 → Level 18
- Solved the challenge by comparing two password files.
- Practiced using:
  ```bash
  grep -vf passwords.new passwords.old
  ```
- Learned how:
  - `-f` reads patterns from a file.
  - `-v` prints lines that do **not** match.
- Introduced to the `diff` command and understood its purpose for comparing files.

---

### Network Enumeration

#### Nmap
- Practiced scanning a range of ports.
- Learned to identify:
  - Open ports
  - SSL/TLS services
  - Echo services
- Used:
  ```bash
  nmap -sV -p 31000-32000 localhost
  ```

---

### SSL/TLS Practice

#### OpenSSL
- Connected to a TLS service using:
  ```bash
  openssl s_client -connect localhost:31790
  ```
- Learned how to interpret:
  - TLS handshake
  - Certificates
  - `read R BLOCK`
  - `KEYUPDATE`
- Troubleshot an OpenSSL client behavior instead of assuming the server was faulty.

---

### SSH Authentication

#### Private Keys
- Retrieved an OpenSSH private key from the Bandit server.
- Understood that some levels use SSH private keys instead of passwords for authentication.

#### SCP
- Practiced transferring files securely between the Bandit server and the Kali machine.
- Corrected the common mistake between:
  - `-p` → Preserve file attributes
  - `-P` → Specify SSH port

---

### Linux File Permissions

#### chmod
- Practiced securing an SSH private key:
  ```bash
  chmod 600 bandit17pass.key
  ```
- Understood why SSH refuses to use private keys with insecure permissions.

---

### SSH Key Files

- Learned that SSH identifies private keys by their **contents**, not by their filename or extension.
- Understood common naming conventions:
  - `id_rsa`
  - `id_ed25519`
  - `.pub`
  - `.key`

---

### Mentor Learning

- Learned to troubleshoot tools before assuming the target system is incorrect.
- Continued strengthening Linux command-line confidence by solving problems independently.
- Improved understanding of Linux permissions, SSH authentication, TLS communication, and file comparison.

---

## 💡 Lessons Learned

- Always enumerate before interacting with services.
- Read and understand Linux error messages.
- Linux permissions directly affect application behavior.
- SSH relies on file contents and permissions—not filename extensions.
- Multiple Linux commands can solve the same problem, but selecting the right tool is an important skill.
- Consistent terminal practice builds confidence faster than memorizing commands.

---

## 🎯 Tomorrow's Goal

- Continue Bandit Level 18.
- Learn the `diff` command in depth.
- Practice more Linux terminal commands without relying on copy-paste.
- Strengthen troubleshooting skills by understanding command outputs before searching for solutions.
