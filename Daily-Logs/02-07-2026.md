# Daily Log - 02-07-2026

## 📚 Today's Learning

### Linux Essentials - Chapter 11 (Basic Scripting)

- Completed the remaining concepts of Chapter 11.
- Learned how **Variables** work in Bash scripts.
- Understood the difference between assigning a variable and accessing it using the `$` symbol.
- Practiced special variables such as `$0`, `$1`, `$2`, and `$?`.
- Learned how to get user input using the `read` command.
- Understood Exit Status (`0 = Success`, `Non-zero = Failure`) and its importance in scripting.

### Conditionals

- Learned the purpose of `if`, `else`, `elif`, and `case`.
- Understood that `if` checks the **Exit Status** of a command instead of its output.
- Learned how Bash automatically executes the `then` block only when the previous command returns Exit Status `0`.
- Learned the `test` command and its shorthand `[ ]`.
- Practiced common comparison operators for files, directories, strings, and numbers.

### Loops

- Learned the difference between `for` and `while` loops.
- Understood when to use each loop:
  - `for` → Known collection or fixed number of items.
  - `while` → Unknown number of repetitions based on a condition.
- Learned the concepts of `-lt` (less than) and Bash arithmetic using `$(( ))`.

### Practical Work

- Created and executed the first Bash script.
- Practiced editing scripts using `nano`.
- Troubleshot script execution errors involving execute permissions (`chmod +x`).
- Strengthened Linux troubleshooting by understanding why errors occurred instead of simply memorizing commands.

## 💡 Lessons Learned

- Programming is based on three core ideas:
  - Store data (Variables)
  - Make decisions (Conditionals)
  - Repeat work (Loops)
- Bash `if` statements depend on a command's Exit Status, not on whether it prints output.
- Understanding the logic behind Bash is more valuable than memorizing syntax.

## 🎯 Tomorrow's Goal

- Begin the next Linux Essentials chapter.
- Continue building Bash scripting confidence through hands-on practice.
- Solve practical scripting challenges to reinforce today's concepts.
