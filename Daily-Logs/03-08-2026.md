# Daily Log - 03-08-2026

## 📚 Today's Learning

### Bandit Level 16 → Level 17

#### Port Enumeration
- Learned how to scan a range of ports using `nmap`.
- Identified all open ports between `31000-32000`.
- Distinguished between normal TCP services and SSL/TLS services.

#### Nmap Service Detection
- Practiced:
  ```bash
  nmap -sV -p 31000-32000 localhost
  ```
- Learned to identify:
  - `echo`
  - `ssl/echo`
  - `ssl/unknown`

#### TLS Connection
- Connected to the SSL service using:
  ```bash
  openssl s_client -connect localhost:31790
  ```
- Reviewed the TLS handshake output.
- Learned that `read R BLOCK` indicates OpenSSL is waiting to receive encrypted application data.

#### OpenSSL Troubleshooting
- Faced the `KEYUPDATE` issue while sending the password.
- Learned that some OpenSSL versions interpret certain keyboard input differently during interactive mode.
- Practiced troubleshooting instead of assuming the server was broken.
- Successfully retrieved the SSH private key after resolving the client behavior.

#### SSH Private Keys
- Learned that the level returns an **OpenSSH Private Key** instead of a password.
- Understood that SSH private keys are used for authentication instead of passwords.

#### SCP (Secure Copy)
- Practiced copying files from the Bandit server to the Kali machine.
- Corrected the mistake between:
  - `-p` (preserve file attributes)
  - `-P` (SSH port)

#### Linux File Permissions
- Learned why SSH requires strict permissions for private keys.
- Practiced:
  ```bash
  chmod 600 bandit17pass.key
  ```
- Understood that SSH rejects private keys that are readable by other users.

#### SSH Key Files
- Learned that SSH does **not** depend on file extensions.
- Understood that SSH recognizes private keys by their **content**, not by filenames such as `.key` or `.pem`.

---

### Bandit Level 17 → Level 18

#### Comparing Files
- Solved the level using:
  ```bash
  grep -vf passwords.new passwords.old
  ```
- Learned:
  - `-f` → Read search patterns from a file.
  - `-v` → Invert the match.
- Understood how `grep` can compare file contents by filtering matching lines.
- Introduced to the `diff` command for comparing two files.

---

## 💡 Lessons Learned

- Enumerate services before interacting with them.
- OpenSSL client behavior can affect communication.
- Read error messages carefully before assuming a service is broken.
- SSH private keys require secure file permissions (`600`).
- Linux identifies SSH keys by their contents, not by file extensions.
- Multiple Linux commands can solve the same problem, but choosing the most appropriate tool is an important skill.

---

## 🎯 Tomorrow's Goal

- Continue Bandit Level 18.
- Learn the `diff` command in depth.
- Strengthen understanding of SSH authentication and Linux file comparison tools.